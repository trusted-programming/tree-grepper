pub struct ExtractedFile<<LIFETIME>'a</LIFETIME>> {
    file: Option<PathBuf<LIFETIME></LIFETIME>>,
    file_type: String,
    matches: Vec<ExtractedMatch<<LIFETIME>'a</LIFETIME>><LIFETIME></LIFETIME>>,
}